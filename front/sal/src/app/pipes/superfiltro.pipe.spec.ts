import { SuperfiltroPipe } from './superfiltro.pipe';

describe('SuperfiltroPipe', () => {
  it('create an instance', () => {
    const pipe = new SuperfiltroPipe();
    expect(pipe).toBeTruthy();
  });
});
