import { Superfiltro2Pipe } from './superfiltro2.pipe';

describe('Superfiltro2Pipe', () => {
  it('create an instance', () => {
    const pipe = new Superfiltro2Pipe();
    expect(pipe).toBeTruthy();
  });
});
